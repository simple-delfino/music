language: node_js
node_js:
  - "8"

install:
  - npm install

script:
  - npm run build

after_success:
  - cd ./dist
  - git init
  - git config --global user.name "${U_NAME}"
  - git config --global user.email "${U_EMAIL}"
  - git add .
  - git commit -m "Automatically update from travis-ci"
  - git push --quiet --force  "https://${GH_TOKEN}@${GH_REF}" master:${P_BRANCH}

branches:
  only:
    - master

env:
  global:
    - GH_REF: github.com/simple-delfino/music.git
    - GH_TOKEN: 473b07b1f638409c0bba5334e2418885087dc7a5
    - P_BRANCH: gh-pages
    - U_EMAIL: 1319879149@qq.com
    - U_NAME: simple-delfino
